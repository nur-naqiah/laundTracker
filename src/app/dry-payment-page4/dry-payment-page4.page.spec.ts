import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DryPaymentPage4Page } from './dry-payment-page4.page';

describe('DryPaymentPage4Page', () => {
  let component: DryPaymentPage4Page;
  let fixture: ComponentFixture<DryPaymentPage4Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(DryPaymentPage4Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
