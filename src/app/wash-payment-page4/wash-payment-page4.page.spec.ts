import { ComponentFixture, TestBed } from '@angular/core/testing';
import { WashPaymentPage4Page } from './wash-payment-page4.page';

describe('WashPaymentPage4Page', () => {
  let component: WashPaymentPage4Page;
  let fixture: ComponentFixture<WashPaymentPage4Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(WashPaymentPage4Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
